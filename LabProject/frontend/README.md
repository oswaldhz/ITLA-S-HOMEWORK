# LabProject Frontend

The frontend targets the API at `https://localhost:7095/api` by default, matching the backend development profile. Override the API base URL by setting `VITE_API_URL` (see `.env.example`) if your backend runs elsewhere.
