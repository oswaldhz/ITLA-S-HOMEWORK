# OpenSong Next

This repository contains the scaffold for a hybrid Qt/FastAPI application that powers control and projection workflows. The `opensong_next` package starts both a PySide6 desktop interface and a FastAPI companion server.
